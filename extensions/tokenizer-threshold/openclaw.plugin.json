{
  "id": "tokenizer-threshold",
  "activation": {
    "onStartup": true
  },
  "kind": "context-engine",
  "name": "Tokenizer Threshold Context Engine",
  "description": "Own threshold compaction with agent-core findCutPoint keep-recent tail (default 113k / 20k); report tokensBefore/tokensAfter to host checkpoints.",
  "uiHints": {
    "thresholdTokens": {
      "label": "Compaction threshold (tokens)",
      "help": "When local tokenizer counts reach this value, assemble returns a summary message plus a keepRecentTokens verbatim tail under the budget."
    },
    "encoding": {
      "label": "Tiktoken encoding",
      "help": "Local encoding loaded by js-tiktoken (default cl100k_base)."
    },
    "keepRecentTokens": {
      "label": "Keep recent tokens",
      "help": "Approximate recent-context token budget kept verbatim after the summary (agent-core findCutPoint default: 20000)."
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "thresholdTokens": {
        "type": "integer",
        "minimum": 1,
        "default": 113000
      },
      "encoding": {
        "type": "string",
        "enum": ["cl100k_base", "o200k_base", "p50k_base", "r50k_base"],
        "default": "cl100k_base"
      },
      "keepRecentTokens": {
        "type": "integer",
        "minimum": 1,
        "default": 20000
      }
    }
  }
}
